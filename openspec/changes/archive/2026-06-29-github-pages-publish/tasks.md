## 1. Configure VitePress base path

- [x] 1.1 Update `src/.vitepress/config.ts` to support dynamic `base` path sub-routing.

## 2. Implement GitHub Actions deployment workflow

- [x] 2.1 Create GitHub Actions workflow file `.github/workflows/deploy.yml` with steps to build and deploy to GitHub Pages.

## 3. Verify and Document

- [x] 3.1 Run a local production build test to ensure base path is handled properly.
- [x] 3.2 Add a deployment section to `README.md` explaining the auto-deploy workflow.
